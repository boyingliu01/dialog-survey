# Admin Authentication Completion Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `subagent-driven-development` or `executing-plans` to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** Finish the deployment-managed single-administrator login flow so all interview-management pages and mutations require a valid session or explicit admin API key, browser mutations enforce CSRF, and sessions expire by inactivity.

**Architecture:** Keep `adminAuth` as the authorization boundary and add one conditional mutation guard that authenticates first, bypasses CSRF only for a successfully validated `X-Admin-Key`, and otherwise invokes Fastify CSRF protection. Render request tokens generated by `reply.generateCsrf()` into the login page and admin shell; remove non-durable application-managed password changes.

**Tech Stack:** TypeScript 5.9, Fastify 5, `@fastify/secure-session`, `@fastify/csrf-protection`, Nunjucks, HTMX, Vitest 4, Playwright 1.61, Biome.

**Design:** `docs/superpowers/specs/2026-08-19-admin-auth-completion-design.md`

**Scope guard:** Never edit, stage, restore, or commit `VERSION` or `reports/*.xlsx` as part of this work.

---

## Task 1: Idle Session Semantics

**Files:**
- Modify: `src/middleware/session-auth.ts`
- Test: `tests/session-auth.test.ts`

- [ ] Add a failing test where an old `loginTime` and recent `lastActivity` remains valid.
- [ ] Add a failing test where a recent `loginTime` and expired `lastActivity` is rejected.
- [ ] Assert successful validation refreshes only `lastActivity` and rejection does not mutate the session.
- [ ] Run `npx vitest run tests/session-auth.test.ts`; expect the new cases to fail because validation uses `loginTime`.
- [ ] Change the expiry comparison to `now - session.lastActivity > maxAgeMs`; retain `loginTime` as immutable audit metadata.
- [ ] Run `npx vitest run tests/session-auth.test.ts` and `npm run type-check`; expect both to pass.

## Task 2: Authentication Ordering and Mode

**Files:**
- Modify: `src/middleware/admin-auth.ts`
- Modify: the narrow Fastify request declaration that owns the authentication-mode discriminant
- Test: `tests/admin-auth.test.ts`

- [ ] Preserve the current uncommitted GET/HEAD redirect tests and removal of the unsafe method exemption.
- [ ] Add failing tests for anonymous browser redirect without configured `ADMIN_API_KEY`, anonymous mutation 401, presented key with missing configuration 500, invalid presented key 401, expired-session deletion, and valid session precedence.
- [ ] Add a typed request discriminant with values `'session' | 'api-key'`; do not infer mode later from header presence.
- [ ] Run `npx vitest run tests/admin-auth.test.ts`; expect the new ordering/deletion cases to fail.
- [ ] Implement this order: valid session, delete invalid existing session, validate explicitly presented key, otherwise redirect GET/HEAD or reject mutation.
- [ ] Leave OPTIONS to Fastify CORS/preflight handling.
- [ ] Run `npx vitest run tests/admin-auth.test.ts tests/session-auth.test.ts` and `npm run type-check`; expect pass.

## Task 3: Conditional CSRF Guard

**Files:**
- Create: `src/middleware/admin-csrf.ts`
- Create: `tests/admin-csrf.test.ts`

- [ ] Add failing tests proving API-key mode skips CSRF, session mode invokes CSRF once, failed authentication never invokes CSRF, and CSRF rejection stops the handler.
- [ ] Run `npx vitest run tests/admin-csrf.test.ts`; expect module-not-found or behavior failure.
- [ ] Implement a typed `createAdminMutationGuard(csrfProtection)` pre-handler: await `adminAuth`, stop if `reply.sent`, skip only for `'api-key'`, otherwise await CSRF protection.
- [ ] Use installed Fastify hook types; no `any`, non-null assertions, or type suppression.
- [ ] Run `npx vitest run tests/admin-csrf.test.ts tests/admin-auth.test.ts` and `npm run type-check`; expect pass.

## Task 4: Login, Logout, and Password-Change Removal

**Files:**
- Modify: `src/routes/admin-auth.ts`
- Modify: `src/views/layouts/admin-tree.njk`
- Delete: `src/views/admin/content/change-password.njk`
- Modify: `tests/admin-auth-routes.test.ts`
- Modify: `tests/login-csrf.test.ts`

- [ ] Replace cost-12 bcrypt hashing in every route-test setup with a stable low-cost fixture computed once.
- [ ] Add failing cases: missing/invalid CSRF login 403, valid-token login reaches credential behavior, tokenless logout 403, valid-token logout clears session, removed password routes 404.
- [ ] Rewrite every existing login/logout POST test to GET a token, preserve its secure-session cookie, then submit both together.
- [ ] Run `npx vitest run tests/admin-auth-routes.test.ts`; expect enforcement/removal cases to fail.
- [ ] Attach `fastify.csrfProtection` to login and the conditional mutation guard to logout.
- [ ] Use `validateSession` for already-authenticated login-page redirects.
- [ ] Delete the password schema, routes, process-env mutation, top-bar action, and fragment template.
- [ ] Run `npx vitest run tests/admin-auth-routes.test.ts tests/login-csrf.test.ts` and `npm run type-check`; expect pass.

## Task 5: Admin Shell Token and Mutation Protection

**Files:**
- Modify: `src/api/admin-templates.ts`
- Modify: browser-driven admin mutations in `src/api/plans.ts`
- Modify: `src/server.ts` only if required for hook composition
- Modify: `src/views/layouts/admin-tree.njk`
- Modify: the smallest relevant admin-template and plan tests

- [ ] Add failing template tests for a rendered meta token, logout token reuse, HTMX header injection, no cookie token read, and no password-change action.
- [ ] Add failing integration cases for session mutation without token 403, session mutation with token reaching application behavior, valid explicit admin key bypass, and invalid key 401.
- [ ] Run the focused suites and confirm failures arise from missing enforcement/token plumbing.
- [ ] Generate a request token in the `/admin` shell route and pass `csrfToken` with `templates` and `adminUser`.
- [ ] Render `<meta name="csrf-token" content="{{ csrfToken }}">`; make global `htmx:configRequest` read that meta element and set `x-csrf-token`.
- [ ] Do not read CSRF material from cookies and do not add per-button `hx-headers`.
- [ ] Apply the conditional mutation guard to all state-changing admin-template routes and browser-driven plan mutations; keep safe GET/download routes on `adminAuth` only.
- [ ] Preserve explicit `X-Admin-Key` automation without browser CSRF.
- [ ] Run focused admin-template/plan tests and `npm run type-check`; expect pass.

## Task 6: Route Regression Coverage

**Files:**
- Modify: `tests/admin-auth-routes.test.ts`
- Modify: `tests/login-csrf.test.ts`
- Modify: selected `tests/admin-templates*.test.ts`
- Modify: selected plan API auth tests

- [ ] Audit every admin POST test: no application success/error assertion may be reached without matching request token and storage cookie unless a valid explicit admin key is used.
- [ ] Keep missing-token and invalid-token tests separate from valid-token setup.
- [ ] Prove token and secure-session storage from different sessions do not match.
- [ ] Prove browser session mode works without `ADMIN_API_KEY` when CSRF is valid.
- [ ] Prove removed password routes remain 404 with no compatibility handlers.
- [ ] Run all focused auth/admin/plan suites; expect no skipped auth tests.

## Task 7: Browser E2E Reliability and Coverage

**Files:**
- Modify: `tests/e2e/admin-auth.e2e.test.ts`
- Modify: `tests/e2e/helpers/e2e-server.ts` only if partial server setup requires it

- [ ] Make server/browser/context/page declarations optional and narrow them after setup; use no non-null assertions.
- [ ] Raise hook timeout to 60 seconds for measured database/browser startup.
- [ ] Cleanup only created resources in reverse order so cleanup cannot mask setup failure.
- [ ] Replace cookie token lookup with login hidden-input and authenticated-shell meta token lookup.
- [ ] Cover anonymous redirect, non-empty login token, valid login and username display, tokenless mutation 403, representative HTMX mutation success, logout, old-session rejection, and API-key compatibility.
- [ ] Run `npx vitest run tests/e2e/admin-auth.e2e.test.ts --reporter=verbose`; expect all scenarios execute and pass.

## Task 8: Password Rotation Documentation

**Files:**
- Modify: `README.md`

- [ ] Add an operator procedure: generate bcrypt hash, update deployment-managed `ADMIN_PASSWORD_HASH`, restart/roll, verify new login.
- [ ] State that the application intentionally has no online password-change route and never writes credentials to `.env`.
- [ ] Correct logout wording to describe form submission rather than GET navigation.
- [ ] Keep `SESSION_MAX_AGE` documented as inactivity duration and `ADMIN_API_KEY` optional unless automation uses it.
- [ ] Include no real passwords, hashes, keys, or secret values.

## Task 9: Final Verification

- [ ] Run focused auth/admin/plan tests; expect pass and no auth skips.
- [ ] Run auth Playwright E2E; expect pass and executed scenarios.
- [ ] Run `npm run type-check`, `npm run check`, and `npm run smoke`; expect exit 0.
- [ ] Run `npx vitest run`; if PostgreSQL infrastructure alone blocks it, report the exact failure instead of claiming full pass.
- [ ] Runtime-check anonymous `/admin` 302, authenticated shell 200, tokenless browser mutation 403, and valid admin-key mutation bypass.
- [ ] Run `git diff --check`; expect no output.
- [ ] Inspect `git status --short` and `git diff --name-only`; confirm pre-existing `VERSION` and report files remain excluded from the authentication diff.
- [ ] Run diagnostics on every changed TypeScript file and inspect every changed file against the design.
- [ ] Do not commit, push, or create a PR unless explicitly requested.

## Execution Order

Tasks 1-3 are sequential because they establish the trust-boundary contract. After Task 3, Tasks 4 and 5 may be delegated in parallel only with disjoint file ownership; otherwise execute sequentially. Tasks 6-8 may run in parallel after route wiring stabilizes. Task 9 runs last.
