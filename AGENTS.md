# AGENTS.md - Interview Bot Project Knowledge Base

> Auto-generated by `/init-deep` on 2026-05-17. Updated 2026-05-30. Score: 8/10.

## Project Overview

**Interview Bot** - AI-powered interview robot that conducts async multi-turn interviews via DingTalk with intelligent follow-up questions, multi-turn context memory, and voice input support.

### Tech Stack

| Component          | Technology                     |
| ------------------ | ------------------------------ |
| Dialog Engine      | Custom LangGraph-inspired workflow (not StateGraph API) |
| LLM Service        | DashScope / Volcengine Ark (deepseek-v3.2) |
| Speech Recognition | Alibaba Fun-ASR                |
| Message Platform   | DingTalk Stream Mode (WebSocket) |
| Database           | PostgreSQL (Prisma ORM)        |
| Web Framework      | Fastify 5.x                    |
| Templating         | Nunjucks (server-side views)   |
| Language           | TypeScript (strict mode, ESM)  |

### Project Stats

- **Source Files**: ~70 TypeScript files (src/)
- **Test Files**: 52+ test files (tests/)
- **Test Coverage**: 80%+ thresholds (lines/functions/statements 80%, branches 70%)
- **Node Version**: >=20.0.0
- **Git**: master branch

---

## Architecture

### Layered Architecture (Clean Architecture)

```
┌─────────────────────────────────────────────────────────────┐
│                     API Layer (Fastify)                      │
│  health.ts, webhook.ts, plans.ts, templates.ts, analysis.ts │
└─────────────────────────────┬───────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                    Services Layer                            │
│  ConversationEngine, AnalysisService, DingtalkService,       │
│  FollowupService, InterviewPlanService, ReportService        │
└─────────────────────────────┬───────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                  Core Layer (LangGraph)                      │
│  StateGraph: planning → interviewing → analyzing → completed │
└─────────────────────────────┬───────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                  Repositories Layer                          │
│  InterviewStateRepository, MessageRepository,                │
│  TemplateRepository (Prisma ORM)                             │
└─────────────────────────────┬───────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│               Integrations Layer                             │
│  DingTalk Client, LLM Base + Volcengine Implementation       │
└─────────────────────────────────────────────────────────────┘
```

### Directory Structure

```
src/
├── api/                    # HTTP endpoints (Fastify routes)
│   ├── analysis.ts         # Analysis/reporting endpoints
│   ├── health.ts           # Health check + Prisma initialization
│   ├── plans.ts            # Interview plan CRUD
│   ├── templates.ts        # Template CRUD
│   ├── admin-templates.ts  # Admin template management (new)
│   └── webhook.ts          # DingTalk webhook handler
│
├── core/                   # Business workflow (LangGraph)
│   ├── graph.ts            # StateGraph definition (custom imperative, NOT StateGraph API)
│   ├── types/index.ts      # InterviewState, GraphConfig types (Zod schema)
│   └── nodes/              # Workflow nodes
│       ├── planning.ts     # Preparation state
│       ├── interviewing.ts # Active conversation
│       ├── analyzing.ts    # Report generation
│       └── completed.ts    # Final state
│
├── services/               # Application services (15 files)
│   ├── conversation-engine.ts    # LangGraph orchestration
│   ├── analysis.service.ts       # Report generation
│   ├── analytics.service.ts      # Analytics data aggregation (KPIs + per-plan stats)
│   ├── dingtalk.service.ts       # Message handling
│   ├── followup.service.ts       # Intelligent follow-ups
│   ├── interview-plan.service.ts # Plan management (save/send decoupled, member CRUD with $transaction)
│   ├── prompt.service.ts         # LLM prompt templates
│   ├── report.service.ts         # Markdown reports
│   ├── asr.service.ts            # Audio-to-text
│   ├── stream-message.service.ts # DingTalk Stream processing (new, 391 lines)
│   ├── batch-aggregation.service.ts # Batch report aggregation (new)
│   ├── dead-letter.service.ts    # Failed analysis retry (new)
│   ├── llm-clustering.ts         # LLM-powered quote clustering (new)
│   └── template-dimension.service.ts # Template dimension analysis (new)
│
├── repositories/           # Data access (Prisma)
│   ├── interview-state.repository.ts  # State persistence + retry + optimistic locking
│   ├── interview.repository.ts        # Interview entity repository (new)
│   ├── message.repository.ts           # Message storage
│   └── template.repository.ts          # Template CRUD
│
├── integrations/           # External services
│   ├── dingtalk/
│   │   ├── client.ts       # DingTalk API client
│   │   ├── stream-client.ts # WebSocket stream client
│   │   └── middleware.ts   # Signature verification
│   └── llm/
│       ├── base.ts         # LLM interface
│       ├── volcengine.ts   # Volcengine Ark implementation
│       └── alibaba.ts      # Alibaba/DashScope implementation (new)
│
├── domains/                # Domain entities (DDD)
│   └── interview/
│       ├── entities/       # Interview, Message, Response
│       └── services/       # Domain logic
│
├── schemas/                # Zod schemas (new)
│   └── dimensions.ts       # Dimension tag schemas
│
├── middleware/             # Express/Fastify middleware (new)
│   └── admin-auth.ts       # Admin API authentication
│
├── views/                  # Nunjucks server-side templates (new)
│   ├── layouts/            # Base layouts
│   ├── admin/              # Admin UI (tree-body.njk + content/)
│   ├── analytics/          # Analytics dashboard
│   ├── reports/            # Report display views
│   └── error.njk           # Error page
│
├── utils/                  # Cross-cutting utilities
│   ├── logger.ts           # Pino wrapper (NO console.log)
│   ├── encryption.ts       # AES encryption
│   ├── rate-limiter.ts     # API rate limiting
│   ├── retry.ts            # Retry wrapper
│   ├── security.ts         # Fastify middleware + PII masking
│   ├── validation.ts       # Input validation
│   └── pii-anonymizer.ts   # PII anonymization (new)
│   └── markdown.ts         # Markdown→HTML renderer with XSS sanitization (marked + walkTokens HTML stripping)
│
├── server.ts               # Main entry point (Fastify factory)
└── index.ts                # Barrel exports (UNUSED - dead code)
```

### Data Model (Prisma)

| Entity           | Purpose                     |
| ---------------- | --------------------------- |
| `Interview`      | Interview session state     |
| `Message`        | Conversation messages       |
| `Response`       | User responses to questions |
| `Template`       | Interview templates         |
| `InterviewPlan`  | Batch interview plans       |
| `AnalysisReport` | Generated reports           |
| `AuditLog`       | Action logging              |

**Key Enums**: `InterviewStatus` (PENDING/ACTIVE/WAITING/COMPLETED/CANCELLED), `SendStatus` (NOT_SENT/SENT/FAILED/DELIVERED), `TemplateStatus`, `PlanStatus`, `BatchReportStatus`, `BatchReportType`

**Recent Schema Additions** (2026-05-30):
- `Interview.sendStatus`, `Interview.sentAt`, `Interview.sendError` - per-member send tracking
- `InterviewPlan.sendStatus` - plan-level send status

---

## Entry Points & Initialization

### Main Entry

**`src/server.ts`** - Fastify app factory and server startup

```typescript
// Initialization sequence:
1. npm run dev → tsx watch src/server.ts
2. Fastify instance created with pino-pretty logger
3. CORS middleware registered
4. securityMiddleware() → PrismaClient initialized
5. Route handlers registered (health, webhook, plans, templates, analysis, admin-templates)
6. Nunjucks view engine configured (src/views/)
7. app.listen({ port: 3000, host: '0.0.0.0' })
```

### Other Entry Points

- **`src/index.ts`** - Barrel exports for all modules
- **`tests/*.test.ts`** - Test entry points (Vitest)

### Environment Setup

Required env vars (see `.env.example`):

- `DATABASE_URL` - PostgreSQL connection
- `VOLCENGINE_API_KEY` - LLM API key
- `VOLCENGINE_MODEL` - Model name (default: deepseek-v3.2)
- `DINGTALK_CLIENT_ID`, `DINGTALK_CLIENT_SECRET` - DingTalk Stream integration
- `FUN_ASR_API_KEY` - Speech recognition
- `ENCRYPTION_KEY` - Security encryption

---

## TypeScript Conventions

### Naming
| Type | Convention | Example |
|------|-----------|---------|
| Classes | PascalCase | `ConversationEngine` |
| Interfaces | PascalCase | `ProcessMessageInput` |
| Functions/Vars | camelCase | `processMessage`, `userId` |
| Private | `private` keyword | `private async loadOrCreateState()` |

### Imports
- Relative imports **require `.js` extension** (ESM): `import { foo } from "../bar.js"`
- `import type` for type-only imports
- Named imports from npm

### Type Rules
- Explicit return types on public functions
- Interfaces for object shapes, type for unions
- Zod schemas for runtime validation (`z.infer` for TS type)
- `noUnusedLocals`, `noUnusedParameters` enforced by tsconfig

### Error Handling
- Custom error classes (`StatePersistenceError`, etc.)
- `instanceof Error` narrowing, never `any`
- Biome: `noConsole: error`, `noExplicitAny: warn`
- Use structured logger: `import { info, error, warn } from "../utils/logger.js"`

---

## Development Workflow

### 全量测试前置条件（CRITICAL）

**执行 `npx vitest --run` 全量回归测试之前，必须确保 PostgreSQL 数据库已启动。**

项目中有大量集成测试依赖真实 Prisma 连接数据库。数据库未启动时，所有集成测试会以 `PrismaClientInitializationError` 失败（表现为 90+ 测试失败），这是环境问题而非代码问题。

```bash
# 检查数据库状态
pg_isready
# 输出: /var/run/postgresql:5432 - accepting connections → 已就绪

# 启动数据库（任选一种）
sudo systemctl start postgresql
# 或
sudo pg_ctlcluster 16 main start

# 确认数据库可连接后再跑测试
npx vitest --run
```

**判定规则**: 如果测试输出中出现 `PrismaClientInitializationError` 或 `ECONNREFUSED`，先检查数据库是否启动，不要盲目改代码。

### Commands

| Command                 | Purpose                             |
| ----------------------- | ----------------------------------- |
| `npm run dev`           | Development server (tsx watch)      |
| `npm run build`         | Compile TypeScript to dist/         |
| `npm run test`          | Run Vitest tests                    |
| `npm run test:coverage` | Tests with coverage (≥80% required) |
| `npm run lint`          | Biome lint on src/                  |
| `npm run lint:fix`      | Auto-fix lint issues                |
| `npm run type-check`    | TypeScript noEmit check             |
| `npm run check`         | Biome check (lint + imports)        |
| `npm run format`        | Biome format                        |

### Quality Gates (Pre-commit Hook)

**全量检查定义**（2026-06-13 更新）："全量检查"指执行 pre-commit hook 的全部 9 个质量门禁 Gate，以及 pre-push hook 的额外验证。**标准远高于默认的 type-check + lint + test**。不仅保证代码正确性，还要保证架构健康度、安全性、技术债务等全面达标。

#### Pre-commit Hook (9 Gates)

| Gate | 名称 | 工具 | 阈值/要求 |
|------|------|------|-----------|
| **Gate 1** | Code Quality (Static + Lint + Shell) | `tsc`, `biome`, `shellcheck` | Must pass |
| **Gate 2** | Duplicate Code Detection | `jscpd` (TS), `cpd` (Java), `dupfinder` (C#), `gomod graph` (Go), `radon cpd` (Python) | Similarity ≤5% |
| **Gate 3** | Cyclomatic Complexity | lizard | warning=5, error=10 |
| **Gate 4** | Principles Checker (Clean Code + SOLID) | SonarQube / language-specific linters | Must pass |
| **Gate 5** | Tests & Coverage | Vitest / pytest / go test / jest | Lines≥80%, Functions≥80%, Branches≥70%, Statements≥80% |
| **Gate 6** | Architecture + Boy Scout Rule | archlint / import-linter / arch-go | Architecture violations = BLOCK; New files: 0 warnings |
| **Gate 7** | IaC Security Scanning | checkov / hadolint / kube-score / tflint | No CRITICAL/HIGH security issues |
| **Gate 8** | Secret Scanning | gitleaks | No secrets detected |
| **Gate 9** | SAST Security Scan | semgrep | No CRITICAL/HIGH vulnerabilities |

#### Pre-push Hook (Additional Gates)

| Gate | 名称 | 工具 | 阈值/要求 |
|------|------|------|-----------|
| **Gate M** | Mutation Testing | stryker-mutator | Mutation score ≥ baseline |
| **Gate M2** | Mock Density Check | Custom script | >50% → BLOCK without justification; >30% → advisory |
| **Code Walkthrough** | Delphi Review Result | `.code-walkthrough-result.json` | Verdict = APPROVED, commit match, not expired |

**执行方式**：
```bash
# Stage changes first, then run pre-commit hook
git add . && bash .git/hooks/pre-commit

# Or use git commit (hook auto-executes)
git commit -m "message"

# Pre-push hook runs on push
git push origin <branch>
```

### 架构检查排除规则（2026-06-13）

**第三方库告警处理原则**：
- `public/js/*.min.js`、`node_modules/` 等第三方压缩库的架构告警（Deep Nesting、Long Parameter List 等）**无需修复**
- 这些是无用功——我们无法控制第三方代码的内部结构
- 如果需要，可通过升级库版本或添加 archlint 配置排除这些文件
- **只有项目自有源代码**（`src/`、`tests/`）需要达到 0 告警状态

### Database Commands

```bash
npx prisma generate  # Generate Prisma Client
npx prisma db push   # Sync schema to database
npx prisma studio    # Open Prisma Studio GUI
```

### Configuration Files

| File               | Purpose                                                                       |
| ------------------ | ----------------------------------------------------------------------------- |
| `tsconfig.json`    | Strict TypeScript, ESNext, noUnusedLocals/Parameters                          |
| `biome.json`       | Linter (noConsole: error, noExplicitAny: warn), 2-space indent, 100 char line |
| `vitest.config.ts` | 80% coverage thresholds, node environment, 10s timeout                        |

---

## Testing

- **Location**: `tests/` directory (flat structure)
- **Files**: 52+ `.test.ts` files by feature
- **Framework**: Vitest 4.x
- **Coverage thresholds**: lines 80%, functions 80%, branches 70%, statements 80%
- **Mocking**: Manual `vi.fn()` Prisma mocks, `vi.mock()` for modules, `vi.stubEnv()` for env vars
- See `tests/AGENTS.md` for detailed patterns

---

## Code Quality Warnings

### Known Issues (Severity: MEDIUM)

**Magic Numbers to Extract:**
| File | Value | Suggested Name |
|------|-------|----------------|
| `src/utils/encryption.ts` | `16` | `IV_LENGTH` |
| `src/repositories/interview-state.repository.ts` | `3` | `MAX_RETRIES` |
| `src/integrations/dingtalk/middleware.ts` | `300000` | `TIMESTAMP_TOLERANCE_MS` |
| `src/api/health.ts` | `5000` | `HEALTH_CHECK_TIMEOUT_MS` |
| `src/services/asr.service.ts` | `30000` | `ASR_TIMEOUT_MS` |
| `src/utils/rate-limiter.ts` | `100`/`1000` | `QUEUE_POLL_INTERVAL_MS`/`RATE_LIMIT_WINDOW_MS` |
| `src/integrations/llm/volcengine.ts` | `0.7`/`2000` | `DEFAULT_TEMPERATURE`/`DEFAULT_MAX_TOKENS` |

**Unused Utilities:**
| File | Purpose | Status |
|------|---------|--------|
| `src/utils/validation.ts` | Input validators (email, phone, URL, UUID) | Never imported |
| `src/utils/encryption.ts` | AES-256-GCM encrypt/decrypt | Never imported |
| `src/utils/rate-limiter.ts` | Token bucket limiter | Never imported externally |

**Large Files (>300 lines):**
| File | Lines | Primary Class | Note |
|------|-------|---------------|------|
| `src/repositories/interview-state.repository.ts` | 390 | InterviewStateRepository | Extract state mapping helpers |
| `src/integrations/dingtalk/stream-client.ts` | 358 | DingTalkStreamClient | WebSocket client with reconnection |
| `src/services/stream-message.service.ts` | 337 | StreamMessageService | Deduplicate class/standalone functions |
| `src/services/interview-plan.service.ts` | 690+ | InterviewPlanService | Save/send decoupled, SendStatus tracking, member CRUD (add/remove/remind) with $transaction |

**TODO Comments:**
| File | Line | Content |
|------|------|---------|
| `tests/template-crud.test.ts` | 19 | `// TODO: Implement with real Prisma` |

### Positive Observations

- ✓ No god classes (>20 methods)
- ✓ No deep nesting (>4 levels)
- ✓ No hardcoded secrets
- ✓ No console.log statements
- ✓ TypeScript strict mode clean
- ✓ Proper error handling with custom classes

---

## Anti-Patterns (THIS PROJECT)

| Pattern                       | Severity  | Enforcement                      |
| ----------------------------- | --------- | -------------------------------- |
| `console.log/warn/error/info` | **ERROR** | Biome rule `noConsole: error`    |
| Explicit `any` type           | **WARN**  | Biome rule `noExplicitAny: warn` |
| Sync file I/O in hot paths    | Avoid     | Use async fs operations          |
| Hardcoded model names         | Avoid     | Use `DEFAULT_MODEL` constant     |
| HTMX swap shell URL into `#main-content` | **ERROR** | Causes nested page bug. Use `/admin/content/*` fragment URLs |
| `new PrismaClient()` in `src/api/`        | **ERROR** | API layer must receive PrismaClient via DI (Fastify `register` opts) from `server.ts`. Routes don't own data-access lifecycle. |
| `prisma.$MODEL.$METHOD()` in `src/api/`   | **ERROR** | ast-grep rule `.ast-grep/rules/no-raw-prisma-in-api.yml` (`npm run lint:prisma`). Route through repositories/services. |
| Public `prisma` getter on service classes | **ERROR** | Encapsulate data access behind explicit service methods (discriminated unions for outcomes). Never expose raw PrismaClient. |

**Enforcement**: Biome linter catches `console.log` and `any` at commit time.

### HTMX URL Convention (CRITICAL)

Admin UI URLs split into two categories:

- **Shell URLs** (return full HTML page with layout): `/admin`, `/admin/templates`, `/admin/plans`, `/admin/analytics`
- **Fragment URLs** (return content-only HTML for HTMX swap): `/admin/content/templates/:id`, `/admin/content/plans/:id`

**Rule**: Any `hx-get`/`hx-post`/`htmx.ajax()` targeting `#main-content` MUST use a fragment URL (`/admin/content/...`). Using a shell URL nests the entire layout inside `#main-content`, producing page-in-page rendering. See `docs/learnings.md` → `fragment-vs-shell-url-naming`.

### Dual Template Set (REMOVED in sprint-2026-05-29-01)

The old `src/views/templates/*` and `src/views/plans/*` full-page template set has been deleted. All admin UI now uses the fragment set at `src/views/admin/content/*` exclusively. When adding new admin pages, create them as HTMX fragments under `admin/content/`.

---

## Integration Points

### DingTalk Integration

- **Webhook**: `/api/webhook.ts` receives DingTalk messages
- **Client**: `src/integrations/dingtalk/client.ts` sends replies
- **Stream Client**: `src/integrations/dingtalk/stream-client.ts` WebSocket real-time messaging
- **Middleware**: Signature verification in `middleware.ts`

### LLM Integration

- **Provider**: Volcengine Ark (deepseek-v3.2) / Alibaba DashScope
- **Interface**: `src/integrations/llm/base.ts`
- **Implementations**: 
  - `src/integrations/llm/volcengine.ts` - Volcengine Ark
  - `src/integrations/llm/alibaba.ts` - Alibaba/DashScope (new)
- **Supported Models**: doubao-seed-2.0-code, doubao-seed-2.0-pro, doubao-seed-2.0-lite, doubao-seed-code, minimax-m2.5, glm-4.7, deepseek-v3.2, kimi-k2.5

### ASR Integration

- **Service**: `src/services/asr.service.ts`
- **Provider**: Alibaba Fun-ASR for voice messages

---

## Common Patterns

### Repository Pattern
```typescript
export class InterviewStateRepository {
  constructor(private prisma: PrismaClient) {}
  async saveState(state: InterviewState): Promise<void> { ... }
  async loadState(userId: string): Promise<InterviewState | null> { ... }
}
```

### LangGraph State Machine
```typescript
// core/graph.ts - Interview workflow
// Custom imperative: PENDING → planningNode → interviewingNode → analyzingNode → completedNode
// NOT using actual LangGraph StateGraph API
```

### Service Layer Pattern
```typescript
export class ConversationEngine {
  constructor(
    private stateRepo: InterviewStateRepository,
    private llm: LLMInterface,
    private dingtalk: DingtalkService
  ) {}
  async processMessage(input: ProcessMessageInput): Promise<ProcessMessageOutput> { ... }
}
```

---

## Quick Reference

### Non-Standard Naming

| Directory   | Standard Alternative | Purpose                     |
| ----------- | -------------------- | --------------------------- |
| `src/api/`  | `src/routes/`        | Fastify HTTP endpoints      |
| `src/core/` | `src/workflow/`      | LangGraph StateGraph engine |

### Service Dependencies

```
followup.service → volcengine.ts + retry.ts + prompt.service
report.service → volcengine.ts + retry.ts + prompt.service
stream-message.service → core/graph.ts + interview-state.repository
conversation-engine → core/graph.ts + core/types
analysis.service → report.service
analytics.service → analysis.service + batch-aggregation.service
batch-aggregation → llm-clustering + dead-letter + analytics
llm-clustering → integrations/llm/base.ts
interview-plan.service → decoupled save/send (SendStatus tracking)
```

### Utility Usage

| Utility       | Imports | Purpose                           |
| ------------- | ------- | --------------------------------- |
| `logger.ts`   | 17      | Structured logging (pino)         |
| `retry.ts`    | 2       | Exponential backoff for LLM calls |
| `security.ts` | 1       | API key middleware                |
| `pii-anonymizer.ts` | new | PII data masking              |
| `markdown.ts` | 1 (server.ts) | XSS-safe Markdown→HTML rendering (Nunjucks `markdown` filter) |

### When Adding New Features

1. **API endpoint** → Add route in `src/api/`, register in `server.ts`
2. **Business logic** → Add service in `src/services/`
3. **Data access** → Add repository in `src/repositories/`
4. **Domain entity** → Add in `src/domains/<domain>/entities/`
5. **Tests** → Add `tests/<feature>.test.ts` with mocked Prisma

### When Debugging

1. Check logs via pino (structured output)
2. Verify environment variables loaded
3. Test Prisma connection via `/health` endpoint
4. Check DingTalk webhook signature verification

---

## 人工验收效率原则（2026-05-24 生效）

### 核心原则
AI 自动化所有能自动化的工作，用户只做必须由人做的事（确认实现是否符合意图）。

### 具体规则
1. **AI 必须预先完成**：数据库启动、服务启动、端口验证、curl 页面测试、类型检查、lint —— 用户验收时页面必须已经能正常打开
2. **用户只做**：在浏览器中人工确认 UI 是否符合预期、确认业务逻辑是否正确
3. **AI 不反复要求用户执行命令**：发现问题 → AI 自己定位 → AI 修复 → AI 验证 → 通知用户结果
4. **缺工具/权限说明**：如果 AI 无法完成（如 sudo 启动 PostgreSQL、访问外部 API），明确列出用户需要执行的操作清单，且只列一次

此原则写入本项目，所有后续 session 必须遵守。

## Karpathy 工程原则（2026-05-25 生效）

源自 Andrej Karpathy 的 LLM 编码观察，针对"改 A 导致 B 被破坏"问题的两条核心原则。

### 3. Surgical Changes（外科手术式改动）

**只碰必须碰的代码。只清理自己制造的混乱。**

- 编辑现有代码时，不"优化"相邻代码、注释或 formatting
- 不重构没坏的东西
- 匹配现有代码风格，即使你更喜欢另一种
- 发现无关的死代码 → **提及但不要删除**（除非用户明确要求）
- 自己的改动产生的 orphaned import/variable/function → 必须清理

**判定标准**: 每一行改动都应能直接追溯到用户的请求。

### 4. Goal-Driven Execution（目标驱动执行）

**定义成功标准。循环直到验证。**

- 把指令转化为可验证的目标：
  - "加验证" → "写非法输入测试 → 让测试通过"
  - "修 bug" → "写复现测试 → 让测试通过"
  - "重构 X" → "确保重构前后测试都通过"
- 多步骤任务时，列出简要计划并标注验证点：
  ```
  1. [步骤] → verify: [检查方式]
  2. [步骤] → verify: [检查方式]
  ```
- **改完任何代码后必须运行 `npm run test` 确认无 regression**
- 强成功标准 → AI 可独立完成闭环；弱标准（"让它跑起来"）→ 需要反复确认

---

### 当 Refactoring

- Follow existing naming conventions
- Use `import type` for type-only imports
- Add `.js` extension to relative imports (ESM)
- Run `npm run type-check && npm run lint` before committing
- Maintain ≥80% test coverage

## AI CODING DISCIPLINE (Karpathy Principles)

**原则 3: Surgical Changes（外科手术式改动）**
- 只碰必须碰的代码。只清理自己制造的混乱。
- 编辑现有代码时，不"优化"相邻代码、注释或 formatting
- 不重构没坏的东西
- 匹配现有代码风格，即使 AI 更喜欢另一种
- 发现无关的死代码 → 提及但不要删除（除非用户明确要求）
- 自己的改动产生的 orphaned import/variable/function → 必须清理
- 判定标准: 每一行改动都应能直接追溯到用户的请求

**原则 4: Goal-Driven Execution（目标驱动执行）**
- 定义成功标准。循环直到验证。
- 把指令转化为可验证目标：
  - "加验证" → "写测试 → 让测试通过"
  - "修 bug" → "写复现测试 → 让测试通过"
  - "重构 X" → "确保重构前后测试都通过"
- 多步骤任务列出验证点
- 改完任何代码后必须运行测试确认无 regression

