# VIEWS KNOWLEDGE BASE

> Auto-generated by `/init-deep` on 2026-06-16. Nunjucks server-side templates.

## OVERVIEW
Nunjucks server-side rendered views for admin UI, analytics dashboard, interview plans, reports, and template management.

## STRUCTURE
```
views/
├── layouts/            # Base layouts
│   ├── admin.njk       # Legacy admin layout
│   └── admin-tree.njk  # Tree-view layout with HTMX + Alpine.js
├── admin/              # Admin interface
│   ├── tree-body.njk   # Admin tree layout
│   └── content/        # Admin content pages (HTMX fragments)
│       ├── plan-detail.njk    # Plan detail with send status
│       ├── plan-form.njk      # Plan create/edit form
│       ├── report-detail.njk  # Report display
│       ├── template-edit.njk  # Template editing
│       ├── template-info.njk  # Template detail view
│       └── template-new.njk   # New template form
├── analytics/          # Analytics dashboard
│   └── index.njk       # Main analytics view
├── reports/            # Report display views
│   ├── detail.njk      # Individual report
│   └── index.njk       # Report list
└── error.njk           # Error page
```

## WHERE TO LOOK
| Task | Location | Notes |
|------|----------|-------|
| Base layout | `layouts/admin-tree.njk` | Tree navigation + HTMX + Alpine.js integration |
| Admin UI fragments | `admin/content/` | All admin pages are HTMX fragments (no layout extends) |
| Analytics | `analytics/index.njk` | Dashboard |
| Reports | `reports/` | Report display views |
| Error page | `error.njk` | Error handling |

## CONVENTIONS
- **Engine**: Nunjucks via `@fastify/view` plugin
- **Autoescape**: `true` (security default)
- **NoCache**: `true` (dev-friendly, consider false for prod)
- **Template dir**: Resolved as `src/views/` in server.ts
- **Admin auth**: Protected by `src/middleware/admin-auth.ts`
- **HTMX fragments**: All `admin/content/*` files are pure fragments (NO `{% extends %}`)
- **Alpine.js**: v3.14.8 loaded in admin-tree.njk, use `$data` not `$set()`

## INTEGRATION
Registered in `server.ts`:
```typescript
await fastify.register(fastifyView, {
  engine: { nunjucks },
  templates: viewsDir,
  options: { autoescape: true, noCache: true },
});
```

## ANTI-PATTERNS (THIS MODULE)
- `noCache: true` in production (performance concern)
- Nunjucks templates not covered by TypeScript type checking
- Admin authentication middleware is separate from view rendering
- Empty catch block in `plan-form.njk:26` swallows JSON parse errors

All admin UI uses HTMX fragments at `src/views/admin/content/*`. Old full-page templates (`templates/`, `plans/`) have been removed.
