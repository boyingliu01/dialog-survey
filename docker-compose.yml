version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: dialog-survey-db
    environment:
      POSTGRES_USER: dialog_survey
      POSTGRES_PASSWORD: dialog_survey_secret
      POSTGRES_DB: dialog_survey
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dialog_survey -d dialog_survey"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dialog-survey-app
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://dialog_survey:dialog_survey_secret@postgres:5432/dialog_survey
      PORT: 3001
      HOST: 0.0.0.0
    env_file:
      - .env
    ports:
      - "3001:3001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped

volumes:
  postgres_data:
